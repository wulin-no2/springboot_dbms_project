server:
  port: 8080

spring:
  #database config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_01
    username: root
    password: wulin9786
  application:
    name: springboot_dbms_project
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB

#firebase storage config - bucket location
firebase:
  bucketLocation: springboot-crud-lina.appspot.com

#mybatis config
mybatis:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
